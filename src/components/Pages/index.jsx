import React from 'react'

export const Inicio = ({index}) => {
    return (
        <div>
            - Inicio ({index})
        </div>
    )
}

export const Tienda = ({index}) => {
    return (
        <div>
            - Tienda ({index})
        </div>
    )
}

export const Carrito = ({index}) => {
    return (
        <div>
            - Carrito ({index})
        </div>
    )
}
